public class CH17_thisKeyword {
    public static void main(String[] args)
    {
        /*
        Understanding this keyword  the this keyword refer to the current object in a class.
        it helps distinguish betweek class attributes and parameter with the same name, calls other constructor, and
        passes the current object as a prameter

        key uses of this keyword
            1   Referencing instance variable
            2   Returning the current class instance
            3   Passing the current object as parameter

            Referencing instance variable
                when parameter share the same name as instance variable, the this keyword distinguish the instance variable
                from the parameter, avoiding confustion
         */
    }

}
